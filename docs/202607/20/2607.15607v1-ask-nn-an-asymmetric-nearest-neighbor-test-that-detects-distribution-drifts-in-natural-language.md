---
title: "ASK-NN: An Asymmetric Nearest-Neighbor Test that detects Distribution Drifts in Natural Language"
title_zh: ASK-NN：一种检测自然语言中分布漂移的非对称最近邻检验
authors: "Sergey Zakharov, Rodion Oblovatny, Alexey Zaytsev"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15607v1"
tags: ["query:llm-halluc"]
score: 9.0
evidence: 通过LLM输出中的分布偏差检测幻觉
tldr: 大语言模型生成的幻觉和人工文本常表现为提示与响应间隐藏状态分布的漂移，且两者长度差异导致样本不对称。本文提出基于有向k近邻图的非对称双样本检验ASK-NN，通过统计混合样本中最近邻同为参考点的参考点数量来衡量分布差异。该统计量在置换零假设下具有精确的有限样本均值与方差，并在固定备择假设下具备渐近正态性与一致性，在多项检测任务中表现优异。ASK-NN计算高效且易于实现，为自然语言处理中非对称样本的分布漂移检测提供了有效且理论完备的新工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15607v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15607v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 684, \"height\": 850, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15607v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 686, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15607v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 834, \"height\": 288, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15607v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 783, \"height\": 563, \"label\": \"Table\"}]"
motivation: LLM生成中的幻觉和人工文本表现为提示与响应隐藏状态分布的偏差，且两者长度差异大，需要非对称的检验统计量。
method: 提出基于有向k近邻图的非对称双样本检验ASK-NN，统计混合样本中最近邻同为参考点的参考点数量。
result: 在置换零假设下具有精确的有限样本均值和方差，且在固定备择假设下渐近正态和一致；在多项检测任务中与基线表现相当。
conclusion: ASK-NN计算高效且易于实现，能有效检测自然语言中的分布漂移，为LLM幻觉和人工文本检测提供了可靠工具。
---

## 摘要
大语言模型（LLM）生成输出中的幻觉和人工文本通常表现为提示词与响应隐状态分布之间的分布偏差。由于提示词或检索到的上下文通常作为参考样本，而响应作为查询样本，且两者在长度上存在显著差异，这种非对称性促使我们采用对这两个样本进行不同处理的变点检验统计量。我们考虑了一种基于有向k-最近邻图的非对称双样本检验ASK-NN。我们的统计量计算在混合样本中其最近邻同样为参考点的参考点数量。在置换原假设下，它具有精确的有限样本条件均值和方差；我们进一步确立了在固定备择假设下的渐近正态性和一致性。ASK-NN计算高效且易于实现。在经验上，它在合成基准、人工文本检测以及基于词元级隐状态的LLM幻觉检测任务中，与基于核和图的基线方法相比具有竞争力。

## Abstract
Hallucinations and artificial text in LLM-generated outputs often appear as distributional deviations between prompt and response hidden-state distributions. Since prompts or retrieved contexts typically serve as reference samples and responses as query samples, with major differences in length, these asymmetries motivate the use of change test statistics that treat the two samples differently. We consider an asymmetric two-sample test ASK-NN based on the directed k-nearest-neighbor graph. Our statistic counts reference points whose nearest neighbor in the pooled sample is also a reference point. Under the permutation null, it admits an exact finite-sample conditional mean and variance; we further establish asymptotic normality and consistency under fixed alternatives. ASK-NN is computationally effective and easy to implement. Empirically, it is competitive with kernel and graph-based baselines on synthetic benchmarks, artificial-text detection, and LLM hallucination detection from token-level hidden states.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：大语言模型（LLM）生成的幻觉和人工文本，在本质上表现为提示词/检索上下文与生成响应之间隐藏状态分布的漂移。
- **核心问题**：在RAG等场景中，上下文通常作为参考样本，响应作为查询样本，两者在长度和角色上存在显著的非对称性。传统的对称双样本检验（如MMD、对称k-NN）将两者互换处理，不仅忽略了参考样本的“锚点”作用，还在样本长度差异大时效率低下。
- **整体含义**：本文将幻觉检测和人工文本检测形式化为一个**非对称双样本检验问题**，提出一种专门针对“参考-查询”不对等结构设计的统计检验方法，以更贴合实际应用需求并提升检测效率。

### 2. 论文提出的方法论
- **核心思想**：提出基于有向k-最近邻图的非对称双样本检验 ASK-NN。不同于对称方法同时统计两个样本内部的近邻重合度，ASK-NN 仅统计**参考样本中，其最近邻也属于参考样本**的点数。
- **关键技术细节与公式**：
  - 设混合样本为 $Z$，参考样本指示函数为 $A(Z_i)$（若为参考点则为1，否则为0），$N_r(Z_i)$ 为 $Z_i$ 的第 $r$ 个最近邻。
  - 统计量定义为：$T_n = \sum_{i=1}^{n} \sum_{r=1}^{k} A(Z_i) A(N_r(Z_i))$。该公式仅计算参考点（$A(Z_i)=1$）的近邻是否也为参考点（$A(N_r(Z_i))=1$）。
- **理论性质**：
  - **精确矩**：在置换原假设下，推导出了 $T_n$ 的精确有限样本条件均值和方差。
  - **渐近正态性**：当样本量趋于无穷且参考样本占比趋于 $\tau$ 时，统计量渐近服从正态分布。
  - **一致性**：在固定备择假设下，证明了其分离泛函最小化当且仅当两分布相同，即检验具有一致性。
- **计算复杂度**：时间复杂度为 $O(n_1(k + nd))$，相比对称方法的 $O(n(k+nd))$ 和核方法的 $O(n^2d)$，在参考样本量 $n_1 \ll n$ 时显著降低计算开销。

### 3. 实验设计
- **数据集/场景**：
  - **合成数据**：高斯分布的均值漂移和方差漂移场景，维度从2到2500。
  - **人工文本检测**：RAID 数据集（GPT-4生成 vs 人类撰写），使用 multilingual-e5-large 提取嵌入。
  - **LLM幻觉检测**：MS MARCO, CNN/DM + Recent News, CoQA 三个数据集，使用 Mistral-7B 和 LLaMA-2-7B 提取 token 级隐藏状态。
- **Benchmark 指标**：检验功效、第一类/第二类错误率、ROC AUC、运行时间。
- **对比方法**：
  - 参数方法：Hotelling’s $T^2$ test。
  - 图方法：Symmetric 1-NN test (Henze, 1988)。
  - 核方法：MMD uB (无偏最大均值差异)。
  - 最优传输：Sinkhorn Divergence (SD)。

### 4. 资源与算力
- 文中仅提及在 **NVIDIA A100 GPU** 上进行了运行时间的扩展性基准测试。
- 由于该方法无需训练（为训练无关的统计检验方法），文中**未明确说明**具体的GPU数量和训练/计算总时长，仅报告了统计量计算的 wall-clock runtime。

### 5. 实验数量与充分性
- **实验数量**：涵盖了3大类场景（合成、人工文本、幻觉），合成数据包含2种漂移类型及多维度，幻觉检测包含3个数据集和2个模型，对比了5种基线，并额外测试了标签置换校准变体（ASK-NN LS）。
- **充分性与客观性**：实验设计较为充分，从理论验证到真实场景层层递进；对比方法涵盖了参数、图、核和最优传输等主流流派，比较客观公平。实验也诚实地暴露了方法在真实数据上的校准问题，并给出了置换检验的补救方案，体现了客观性。

### 6. 论文的主要结论与发现
- ASK-NN 在高维方差漂移检测中表现最强，而对称1-NN检验几乎完全失效。
- 在人工文本检测中，ASK-NN 能捕捉分布差异，但其渐近正态校准在真实文本嵌入上存在第一类错误膨胀问题，需依赖标签置换来保证校准的可靠性。
- 在 LLM 幻觉检测中，ASK-NN 在 6 个设置中的 5 个取得了最佳或次佳的 ROC AUC，整体优于 MMD 等基线。
- 计算效率上，ASK-NN 的运行时间增长远慢于 MMD 和 SD，且比对称 k-NN 略快，具有更好的实际可扩展性。

### 7. 优点
- **理论扎实**：为非对称统计量严格推导了有限样本矩、渐近正态性和一致性，理论贡献完整。
- **设计贴合实际**：非对称计数规则完美契合 RAG 场景中“参考上下文-生成响应”的不对等地位。
- **计算高效**：复杂度仅与参考样本量线性相关，在大上下文场景下比核方法更具扩展性。
- **对尺度变化敏感**：在高维数据的方差漂移检测上显著优于传统对称图检验。

### 8. 不足与局限
- **校准失效风险**：在真实高维、各向异性的文本嵌入上，渐近正态校准不准确，必须退回使用计算成本更高的置换检验，削弱了其理论校准的实用价值。
- **独立性假设**：理论推导基于 i.i.d. 假设，但 LLM 的 token 级隐藏状态存在强局部依赖，当前理论未覆盖依赖数据，可能导致检验过于 liberal。
- **均值漂移检测非最优**：在合成数据的均值漂移实验中，表现不如专门针对均值差异的 Hotelling’s $T^2$ 和 MMD。

（完）
