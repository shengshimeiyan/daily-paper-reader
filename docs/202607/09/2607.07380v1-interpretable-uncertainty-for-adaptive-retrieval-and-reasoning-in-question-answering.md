---
title: Interpretable Uncertainty for Adaptive Retrieval and Reasoning in Question Answering
title_zh: 面向问答的自适应检索与推理的可解释不确定性
authors: "Ritajit Dey, Iadh Ounis, Graham McDonald"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07380v1"
tags: ["query:llm-halluc"]
score: 8.0
evidence: 通过不确定性感知的RAG减轻幻觉
tldr: 大语言模型在问答中易产生幻觉且缺乏透明度，现有RAG的检索决策通常基于不透明策略或低效的多步提示。本文提出一种基于LLM内部表征不确定性信号的自适应问答框架，在单次前向传播中区分并估计知识不足与知识模糊或冲突。系统根据信号指导行为：知识不足时触发RAG检索，知识模糊或冲突时应用额外推理。该方法为现有检索与推理策略提供了透明且实用的替代方案，支持可解释的用户端工具设计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07380v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 大语言模型易产生幻觉且缺乏透明度，现有RAG的检索决策通常不透明或计算低效。
method: 提出基于LLM内部表征的不确定性感知框架，单次前向传播中区分并估计知识不足与模糊冲突信号。
result: 知识不足时触发RAG检索，知识模糊或冲突时应用额外推理，实现高效自适应决策。
conclusion: 该方法为自适应检索与推理提供了透明实用的替代方案，支持设计可解释的用户端工具。
---

## 摘要
大型语言模型在问答（QA）中取得了优异的表现，但仍然容易产生幻觉，且透明度有限。检索增强生成（RAG）可以提高事实性，但关于何时以及如何从外部资源检索的决策通常基于不透明的策略或计算效率低下的多步提示过程。我们提出了一种基于源自LLM内部表示的显式信号的自适应问答不确定性感知框架。我们区分了知识不足与知识歧义或冲突，并在单次前向传播中从隐藏状态高效地估计它们。这些信号指导系统行为：当知识不足时触发RAG，而当歧义或冲突较高时应用额外的推理。通过将自适应决策建立在分解且高效可估计的不确定性信号之上，该方法为现有的检索和推理策略提供了一种透明且实用的替代方案，支持可解释的面向用户工具的设计。

## Abstract
Large language models (LLMs) achieve a strong performance in question answering (QA), but remain prone to hallucinations and suffer from limited transparency. Retrieval-augmented generation (RAG) can improve factuality, yet decisions about when and how to retrieve from external resources are typically based on opaque policies or computationally inefficient multi-step prompting procedures. We propose an uncertainty-aware framework for adaptive QA based on explicit signals derived from LLM internal representations. We distinguish between knowledge insufficiency and knowledge ambiguity or conflict, and efficiently estimate these from hidden states in a single forward pass. These signals guide system behaviour: RAG is triggered when knowledge is insufficient, while additional reasoning is applied when ambiguity or conflict is high. By grounding adaptive decisions in decomposed and efficiently estimable uncertainty signals, this approach provides a transparent and practical alternative to existing retrieval and reasoning strategies supporting the design of interpretable user-facing tools.

---

## 论文详细总结（自动生成）

# 论文总结：面向问答的自适应检索与推理的可解释不确定性

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大语言模型（LLM）在问答（QA）中易产生幻觉且缺乏透明度。虽然检索增强生成（RAG）能提升事实性，但现有的自适应检索决策机制通常基于不透明的隐式策略或计算低效的多步提示，且未能区分模型不确定性的来源。
- **整体含义**：并非所有问题都适合检索——当模型知识不足时需要外部检索，但当模型内部知识存在歧义或冲突时，检索可能引入噪声，此时更需要增加推理计算来甄别竞争性答案。因此，需要一种高效、可解释的方法来区分不确定性的类型，并据此指导系统的检索或推理行为。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：提出一种基于LLM内部表征（隐藏状态）的不确定性感知框架，在单次前向传播中区分并估计两种不确定性信号：知识不足与知识模糊/冲突，并以此驱动自适应决策。
- **关键技术细节**：
  - **不确定性估计**：在LLM隐藏状态上训练轻量级回归探针。
    - **知识不足**：定义为事实在LLM预训练语料库中的出现次数。出现次数越少，知识不足程度越高。探针任务为从隐藏状态预测该出现次数。
    - **知识模糊/冲突**：定义为事实不同版本在预训练语料库中出现次数的熵。熵越高，表示内部竞争性答案越多，模糊/冲突程度越高。探针任务为从隐藏状态预测该熵值。
  - **自适应决策机制**：基于阈值的简单路由。
    - 若**知识不足**超过阈值 -> 触发 RAG 检索外部信息。
    - 若**知识模糊/冲突**较高 -> 触发额外推理（如 Chain-of-Thought 或 Self-Consistency）以解决竞争。
    - 若两者均低 -> 模型直接生成答案，无需额外干预。

## 3. 实验设计：数据集、Benchmark 及对比方法
- **数据集/场景**：事实类问答，使用 Natural Questions (NQ) 数据集。
- **使用模型**：Llama-2-7b-chat。
- **评估指标/Benchmark**：使用语义熵和加权熵产生率作为选择性触发 RAG 的评估指标，并使用 McNemar 检验验证统计显著性。
- **对比方法**：
  - LLM-only 基线（无检索无额外推理）。
  - Always-on RAG 基线（始终触发检索）。

## 4. 资源与算力
- **说明**：本文为短文（3页），文中未明确提及所使用的算力资源（如 GPU 型号、数量、训练时长等）。

## 5. 实验数量与充分性
- **实验数量**：仅展示了初步实验结果。在单一数据集（NQ）、单一模型（Llama-2-7b-chat）上进行了测试，对比了 2 种基线方法，使用了 2 种评估指标。未提及消融实验（如不同阈值的影响、不同探针层的影响等）。
- **充分性与客观性**：实验客观性较好（使用了统计显著性检验），但**充分性明显不足**。作为一篇提出新框架的论文，缺乏多数据集、多规模模型的验证，且未对核心组件（如两种不确定性信号的解耦贡献）进行消融分析，难以全面评估方法的泛化性与各模块的实际效用。

## 6. 论文的主要结论与发现
- 将不确定性分解为“知识不足”和“知识模糊/冲突”是可行且有利的，能够为系统提供可解释的决策依据。
- 基于隐藏状态的单次前向传播探针可以有效估计这两种不确定性，避免了昂贵的多次采样或辅助模型。
- 初步实验表明，该不确定性驱动的自适应 QA 框架显著优于 LLM-only 基线（SE +5.9%, WEPR +4.7%）和 Always-on RAG 基线（SE +3.3%, WEPR +2.1%）。
- 可解释性应作为 QA 系统的设计原则而非事后补救工具。

## 7. 优点：方法或实验设计上的亮点
- **细粒度的不确定性解耦**：精准区分了“不知道”（需检索）和“知道但存在冲突”（需推理），这比笼统的不确定性估计更符合实际 QA 场景的干预需求。
- **极高的计算效率**：利用单次前向传播的隐藏状态提取信号，避免了 Self-RAG 等方法的多步反思或多次采样带来的高延迟。
- **原生可解释性**：决策过程直接对应明确的知识状态（缺失 vs 冲突），使得系统的路由行为（为何检索、为何推理）对用户和系统设计者而言都是透明可解释的。

## 8. 不足与局限
- **实验覆盖度低**：仅在单一数据集和小参数模型上进行了初步验证，缺乏对更大模型或复杂推理数据集的测试。
- **探针训练的先验依赖**：探针的训练目标依赖于事实在“预训练语料库中的出现次数及其熵”，而这些统计量对于大多数 LLM（尤其是闭源模型）是未知的或极难精确获取的，这限制了方法的通用性。
- **决策机制的单一性**：采用简单的阈值路由机制，对于现实中“既缺乏部分知识又存在内部冲突”的复合型复杂问题，可能缺乏灵活的联合处理能力。

（完）
